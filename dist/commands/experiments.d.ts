/**
 * Experiment commands — analyze A/B tests and feature flags.
 * All via MCP server (OAuth).
 */
import { Command } from "commander";
export declare function registerExperimentCommands(program: Command): void;
